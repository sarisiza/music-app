package com.example.musicapp.utils

/**
 * Defines an enum for each of the Genres
 */
enum class Genres(val genre: String) {

    ROCK("rock"), //rock genre
    POP("pop"), //pop genre
    CLASSIC("classick") //classic genre

}